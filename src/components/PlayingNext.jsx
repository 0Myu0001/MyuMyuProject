import * as React from 'react';
import Box from '@mui/material/Box';

const PlayingNext = () => {
  return(
    <Box sx={{
      gridArea: '13 / 5 / 25 / 7',
    }}>
      {/* セッションから前に再生した曲と後に再生する曲のデータを取得して表示 */}
    </Box>
  );
};

export default PlayingNext;
